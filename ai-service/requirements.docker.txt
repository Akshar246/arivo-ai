
# ─────────────────────────────────────────────────────────────
# Arivo AI Service — Docker-specific requirements
# This is NOT the full pip freeze. It is the minimal set that
# main.py and job_sync.py actually need. Keeping it lean means
# your Docker image stays under 1GB instead of 4-5GB.
# ─────────────────────────────────────────────────────────────
 
# Web framework
fastapi==0.136.3
uvicorn==0.48.0
 
# Environment + config
python-dotenv==1.2.2
 
# AI / LLM
langchain==1.3.2
langchain-core==1.4.0
langchain-groq==1.1.2
langchain-huggingface==1.2.2
langchain-chroma==0.2.4
langchain-community==0.4.2
 
# Groq LLM client
groq==0.37.1
 
# Vector store
chromadb==1.5.9
 
# Embeddings (sentence-transformers pulls what it needs, not full torch)
sentence-transformers==5.5.1
 
# PDF extraction
pdfplumber==0.11.5
 
# HTTP requests (for Adzuna + Home Office CSV)
requests==2.34.2
 
# ML model (skill gap scorer)
joblib==1.5.3
scikit-learn==1.8.0
 
# Data validation
pydantic==2.13.4
python-multipart==0.0.20
